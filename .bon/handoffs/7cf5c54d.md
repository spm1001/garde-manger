# Handoff — 2026-03-22

session_id: 7cf5c54d-c38f-4da0-bf2e-87a2e77495f4
purpose: Made garde-manger a self-contained plugin with hooks

## Done
- Made garde-manger a fully self-contained Claude Code plugin (0.3.0 → 0.3.1)
- Added SessionStart hook (ensure-garde.sh) — CLI check + version alignment
- Added SessionEnd hook (session-end.sh) → `garde ingest-session` command
- `garde ingest-session --session-id X --cwd Y` — single Python entry point replacing shell orchestration
- Auto-migrating DB path: legacy `~/.claude/memory/` → plugin data dir with symlink
- Hezza migration complete: DB moved, plugin installed, CLI at 0.3.1
- Cron backfill updated to bill via Vertex (CLAUDE_CODE_USE_VERTEX=1)
- Backfill running on Vertex for ~1,697 unextracted sessions
- Review fixes: WAL mode, cross-fs shutil.move, hook logging, unused import, encode_cwd location
- 128 tests (6 new for ingest-session)
- Cross-repo contract documented: bon /close ↔ garde ingest-session extraction schema
- plugin-capabilities.md updated to reflect garde's new hooks

## Gotchas
- The `ensure-garde.sh` version check uses string comparison on grep output — no automated test for the shell parsing
- Session-end hook relies on `session_id` field in Claude Code's hook stdin JSON — format inferred from bon's hook, not from CC docs
- The 200-byte minimum size check in ingest-session could skip legitimate tiny sessions permanently (they never enter the DB for backfill to find)
- Backfill is running in background on Vertex (PID may have changed) — check `ps aux | grep "garde backfill"`

## Risks
- Staged extraction contract spans two repos (bon + garde). Cross-references added to both CLAUDE.md and SKILL.md, but schema changes still require manual coordination
- WAL mode was just enabled — existing DB was in journal mode. First concurrent access triggers the WAL transition. Should be fine but worth monitoring `~/.claude/logs/garde.log`

## Next
- `garde doctor` command — self-test for hooks, CLI version, DB, cron, pending-extractions
- Ardoise test could be more thorough (tested CLI layer, couldn't test actual plugin install lifecycle)
- Consider PreCompact hook (H2) to preserve garde context across compaction

## Commands
```bash
# Check backfill progress
tail -20 /tmp/garde-backfill-vertex.log
# Check session-end hook logs
cat ~/.claude/logs/garde.log
# Verify DB is at new location
ls -la ~/.claude/memory/memory.db ~/.claude/plugins/data/garde-manger-batterie-de-savoir/memory.db
```

## Reflection
**Claude observed:** The biggest design win was `garde ingest-session` — moving all logic from fragile bash string manipulation into Python where error handling is sane. The auto-migration was surprisingly smooth (one `garde status` call triggered it). The stress-test round ("what did we miss?") caught real issues (no WAL mode, cross-fs rename, silent hook failures) that would have bitten future sessions.
**User noted:** Vertex billing via inherited env vars was simpler than a garde-specific flag — just set the same env vars as the claudev alias. Also pushed for fixing all review findings immediately rather than filing them as future work, which was the right call — no future Claude would have the same context.
